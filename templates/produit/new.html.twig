
{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        <div class="row">
            <div class="col">{{ form_row(form.nom) }}</div>

        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}

{% block title %}
    Form
{% endblock %}

